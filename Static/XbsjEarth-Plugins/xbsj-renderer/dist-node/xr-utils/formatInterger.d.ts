/**
 *
 * @param num 传入的数字
 * @param n 需要的字符长度
 * @returns
 * @example
 * formatInterger(722, 5) //"00722"
 *
 */
export declare function formatInterger(num: number, n: number): string;
export declare function getDateTime(): string;
