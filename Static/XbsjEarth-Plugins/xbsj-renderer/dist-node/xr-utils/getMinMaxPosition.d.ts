export declare function getMinMaxPosition(positions: number[], target?: {
    min?: [number, number, number];
    max?: [number, number, number];
}): {
    min: [number, number, number];
    max: [number, number, number];
};
